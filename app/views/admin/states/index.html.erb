<%= link_to "New State", new_admin_state_path %>

<div id='states'>
  <% @states.each do |s| %>
    <div class= "<%= s.name %>">
      <%= s.name %>
      <%= link_to "Make Default", make_default_admin_state_path(s) %>
    </div>
  <% end %>
</ul>
